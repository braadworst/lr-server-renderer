{
  "name": "lr-server-renderer",
  "version": "1.0.0",
  "description": "Server side renderer for Lagoon road",
  "main": "index.js",
  "scripts": {
    "test": "nyc tape test/*.js | tap-spec",
    "report": "nyc report --reporter=html",
    "coverage": "nyc report --reporter=text-lcov | coveralls",
    "all": "npm run test && npm run report && npm run coverage"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lagoon-road/lr-server-renderer.git"
  },
  "keywords": [
    "server",
    "renderer",
    "lagoon",
    "road"
  ],
  "author": "Roy Niels",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/lagoon-road/lr-server-renderer/issues"
  },
  "homepage": "https://github.com/lagoon-road/lr-server-renderer#readme",
  "dependencies": {
    "cheerio": "^0.22.0"
  },
  "devDependencies": {
    "coveralls": "^2.13.0",
    "nyc": "^10.2.0",
    "tap-spec": "^4.1.1",
    "tape": "^4.6.3"
  }
}
